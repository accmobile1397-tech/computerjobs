services:
  mysql:
    deploy:
      resources:
        limits:
          memory: 1G
    restart: always

  redis:
    deploy:
      resources:
        limits:
          memory: 256M
    restart: always

  minio:
    deploy:
      resources:
        limits:
          memory: 512M
    restart: always
