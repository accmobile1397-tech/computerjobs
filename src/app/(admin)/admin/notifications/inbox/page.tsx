import { AdminNotificationInboxClient } from "./admin-notification-inbox-client";

/** P10-013 — inbox viewer (C-009-6 GET only). */
export default function AdminNotificationInboxPage() {
  return <AdminNotificationInboxClient />;
}
