import { AdminNotificationDeliveriesClient } from "./admin-notification-deliveries-client";

/** P10-013 — deliveries viewer (GET only). */
export default function AdminNotificationDeliveriesPage() {
  return <AdminNotificationDeliveriesClient />;
}
