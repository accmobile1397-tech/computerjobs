import { AdminPagePlaceholder } from "../_components/admin-page-placeholder";

export default function AdminBillingPage() {
  return (
    <AdminPagePlaceholder
      title="صورتحساب"
      description="مدیریت پلن و تنظیمات از طریق /api/v1/admin/billing"
    />
  );
}
