/** Phase 9 notification IAM slugs (P9-014). */
export const NOTIFICATION_PERMISSIONS = {
  READ_OWN: "notifications:read:own",
  PREFERENCES_OWN: "notifications:preferences:own",
  ADMIN: "notifications:admin",
} as const;
